name: vertical_span
abstract: true

fields:
  - name: base_level_id
    type: reference
    reference: level
    description: Base level. Optional — usually derived from the containing directory, but required for multi-story elements stored in global/.

  - name: top_level_id
    type: reference
    reference: level
    description: Top constraint level. Empty = next level above current level.

  - name: top_offset
    type: float
    default: 0
    description: Offset from top level. Default 0.

  - name: height
    type: float
    computed: true